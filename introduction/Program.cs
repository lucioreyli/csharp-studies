using static System.Console;
// using IntroductionVariablesTypes;
using DataStructure;

namespace Introduction {
  class Program {
    static void Main(string[] args) {
      // Introduction to data types and supported values
      // new Class1().Init();
      // new Class2().Init();
      // new Class3().Init();

      // Introduction to data structure
      // new ArrayExample().Init();
      // new StackExample().Init();
      new QueueExample().Init();
    }
  }
}
